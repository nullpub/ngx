import { AsyncDataDirective } from './async-data.directive';

describe('AsyncDataDirective', () => {
  it('should create an instance', () => {
    const directive = new AsyncDataDirective();
    expect(directive).toBeTruthy();
  });
});
